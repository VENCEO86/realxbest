# 📊 데이터 표시 개수 증가 수정

## 🚨 발견된 문제

### 현재 설정
- ❌ API 기본 limit: **20개**
- ❌ API 최대 limit: **30개** (제한됨)
- ❌ 프론트엔드 최대 limit: **30개** (제한됨)
- ❌ 카테고리별 limit: **100개**

### 문제점
- 각 카테고리 선택 시 **20개만** 표시됨
- 광고 삽입을 위해 충분한 데이터가 없음
- 사용자 요구사항(최소 200개 이상) 미달

---

## ✅ 수정 사항

### 목표 설정
- ✅ 기본 limit: **200개**
- ✅ 최대 limit: **500개** (광고 삽입 고려)
- ✅ 카테고리별 limit: **200개**

### 수정된 파일

#### 1. `app/api/rankings/route.ts`
**변경 전:**
```typescript
// limit 최적화: 성능을 위해 최대 30개로 제한 (초기 로딩 속도 향상)
const requestedLimit = parseInt(searchParams.get("limit") || "20");
const limit = Math.min(requestedLimit, 30); // 최대 30개로 제한
```

**변경 후:**
```typescript
// limit 설정: 광고 삽입을 위해 충분한 데이터 필요 (최소 200개 이상)
const requestedLimit = parseInt(searchParams.get("limit") || "200");
const limit = Math.min(requestedLimit, 500); // 최대 500개로 제한 (광고 삽입 고려)
```

#### 2. `components/ranking/RankingTable.tsx`
**변경 전:**
```typescript
// limit 최적화: 성능을 위해 최대 30개로 제한 (초기 로딩 속도 향상)
const limit = Math.min(
  parseInt(searchParams.get("limit") || "20"),
  30 // 최대 30개로 제한
);
```

**변경 후:**
```typescript
// limit 설정: 광고 삽입을 위해 충분한 데이터 필요 (최소 200개 이상)
const limit = Math.min(
  parseInt(searchParams.get("limit") || "200"),
  500 // 최대 500개로 제한 (광고 삽입 고려)
);
```

#### 3. `components/category/CategoryRanking.tsx`
**변경 전:**
```typescript
const response = await fetch(`/api/rankings?category=${category}&limit=100`);
```

**변경 후:**
```typescript
const response = await fetch(`/api/rankings?category=${category}&limit=200`);
```

---

## 📊 수정 후 예상 동작

### 기본 동작
- 카테고리 선택 시 **200개** 채널 표시
- 페이지네이션으로 추가 데이터 로드 가능
- 최대 **500개**까지 표시 가능

### 광고 삽입 고려
- 충분한 데이터 풀로 광고 삽입 가능
- 중간 중간 광고 배치 가능
- 사용자 경험 개선

---

## 🚀 배포 후 확인

### 1. 카테고리별 데이터 확인
- 각 카테고리 선택 시 200개 표시 확인
- 페이지네이션 작동 확인

### 2. API 엔드포인트 테스트
```bash
# 기본 요청 (200개)
curl "https://realxbest.com/api/rankings?category=엔터테인먼트"

# 최대 limit 테스트 (500개)
curl "https://realxbest.com/api/rankings?category=엔터테인먼트&limit=500"
```

### 3. 웹사이트 확인
- URL: https://realxbest.com
- 카테고리 선택 시 200개 표시 확인
- 페이지네이션 버튼 작동 확인

---

## 📋 변경사항 요약

| 항목 | 변경 전 | 변경 후 |
|------|---------|---------|
| API 기본 limit | 20개 | **200개** |
| API 최대 limit | 30개 | **500개** |
| 프론트엔드 기본 limit | 20개 | **200개** |
| 프론트엔드 최대 limit | 30개 | **500개** |
| 카테고리별 limit | 100개 | **200개** |

---

## 💡 참고사항

- 데이터베이스에 충분한 데이터가 있어야 함
- 성능 최적화를 위해 페이지네이션 사용 권장
- 광고 삽입을 위해 충분한 데이터 풀 확보
- 필요시 추가 최적화 가능


